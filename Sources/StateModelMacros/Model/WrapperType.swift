
/// All available wrapper types to detect during macro generation
enum WrapperType: String {
    case property = "Property"
    case reference = "Reference"
    case listReference = "ReferenceList"
}

